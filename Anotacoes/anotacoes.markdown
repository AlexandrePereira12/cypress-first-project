Iniciando projeto:

    - npm init --yes

Instalando o cypress:

    -npm install -D cypress@12.5.0

Abrindo o cypress:

    -npx cypress open

Rodando arquivo cypress

    -npx cypress run

    -npm test